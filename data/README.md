# Data provenance

Place an authorized credit-risk dataset in `data/raw/`, which is ignored by Git. Record its source, license, retrieval date, target definition and sensitive attributes. Never commit applicant-level records.
